# Журнал зарядки EV — что сделано, как и почему

## Зачем это вообще понадобилось

История зарядок в приложении зарядника ("X Smart EVSE") — единственный источник данных о кВтч, стоимости и длительности сессий. Полагаться на то, что приложение никогда не сбросит и не забудет эту историю, нельзя. Нужен был независимый журнал.

## v1 — черновик внутри Claude

Первая версия была собрана как артефакт Claude (`window.storage`, публикация по ссылке). На этом этапе выяснились два ограничения платформы:

- Хранилище артефакта работает только у **опубликованной** версии — черновик/превью в чате данные не сохраняет.
- Каждая новая публикация после правки кода создаёт **новую ссылку с пустым хранилищем** — обновить существующую "на месте" нельзя. Из-за этого был добавлен экспорт/импорт CSV как страховка перед каждой правкой.

## v2 — переезд на git + собственное облако

Решили не оставаться в этой модели и вынесли файл (`index.html`) в собственный git-репозиторий. Хранение переделали с `window.storage` на **JSONBin.io**:

- Один общий "bin" (хранилище) зашит в файл (`BUILT_IN_BIN_ID`), у каждого устройства — свой ввод **Master Key** (один раз при первой настройке, хранится в `localStorage` этого браузера).
- Все устройства с одним и тем же ключом видят один и тот же журнал — так синхронизация работает и на телефоне, и на компьютере, и уже подтверждена рабочей в оба конца.
- Дополнительно ведётся локальный кэш (`localStorage`) — если нет связи с облаком, журнал показывает последние загруженные данные и продолжает работать.
- Индикатор синхронизации (точка рядом с датой) показывает статус: синхронизировано / идёт сохранение / нет связи — локально / нет ключа.

Почему так: постоянная ссылка (не меняется при правках кода), не зависит от плана подписки Claude, и данные общие на несколько устройств "из коробки" — ровно то, чего не хватало в v1.

## Функциональность на сегодня

- Ввод сессии: дата, энергия (кВтч), цена за кВтч (подставляется последняя введённая), время начала/окончания, пробег (одометр, км — общий счётчик машины, не суточный трип), заметка.
- Автоматический расчёт стоимости и средней мощности сессии.
- Редактирование существующей записи (не только добавление/удаление).
- Удаление — через подтверждение ("Удалить? Да / Нет"), не мгновенно.
- Экспорт/импорт CSV — независимый бэкап и способ массового редактирования. Экспорт включает служебную колонку **ID** (первая). Импорт сопоставляет строки **гибридно**: сначала по ID (совпал — обновляет ту запись на месте, что бы в ней ни поменялось), при пустом ID — по естественному ключу **дата + время начала** (задаётся один раз при постановке на зарядку и не меняется). Совпавшая запись **обновляется**, не совпавшая — добавляется в конец; повторный импорт того же файла ничего не меняет (идемпотентность). Так можно править цену/пробег/заметку в Excel или дозаполнять запись по частям, не плодя дубли.
- Сводка: суммарные кВтч (с анимацией), число сессий, итоговая стоимость, средние кВтч и мощность по сессиям.
- **Графики — свайп-карусель по месяцам.** Один блок с переключаемыми типами графиков: вкладки во всю ширину («Расход | 100 км | Мощность») + точки-индикатор позиции; смена типа тапом по вкладке или **свайпом** влево/вправо по графику (на телефоне пальцем, на компе перетаскиванием мышью). Каркас рассчитан на рост числа графиков — свайп не упирается в ширину экрана.
  - **Листание по годам:** график показывает один год за раз (максимум 12 столбцов, Jan–Dec). Стрелки ‹ › листают годы подряд (гаснут на крайних), кнопка «тек. год» прыгает на текущий календарный год. Отдельной строкой над графиком — годовая сводка «2026 · N кВтч · N ₪», меняется вместе с выбором года.
  - **График «Расход» (единственный реальный на данный момент):** кВтч столбиками (медь) + стоимость ₪ линией с точками (индиго). Значения кВтч подписаны над столбиком, ₪ — под точкой линии (разведены, чтобы не сливались). Месяцы — английские трёхбуквенные (Jan/Feb…), подписи наклонены на 45°; год на оси не дублируется (он в годовой сводке). Стоимость ₪ округляется **вверх** (`Math.ceil`) и в подписях, и в годовой сумме — цифры на графике и в сводке согласованы.
  - **Графики «100 км» и «Мощность» — заглушки** («Этот график появится позже»): каркас готов, наполнение — по мере накопления данных пробега.
  - Собственный SVG на чистом JS, без внешних библиотек и интернета для отрисовки — не зависит от CDN.
- Оформление отсылает к реальному электрощитку — пронумерованные "слоты" записей как позиции автоматов, зелёный индикатор как LED на автоматах, медно-янтарный акцент в цвет медной шины щитка.

## Фикс: формат времени

Поля времени начала/окончания сессии первоначально были нативными `<input type="time">` — а такой инпут показывает 12- или 24-часовой формат в зависимости от локали браузера/ОС, а не от самого HTML. Универсального атрибута "покажи 24 часа" не существует.

Заменили на пару простых числовых полей **ЧЧ / ММ** для начала и для окончания — всегда 24-часовой ввод, без сюрпризов на любом устройстве. Внутри данные по-прежнему хранятся строкой "ЧЧ:ММ" — остальная логика (расчёт длительности, CSV) не менялась.

## Решили НЕ делать: автоматическое подтягивание тарифа IEC

Возник вопрос — можно ли автоматически вытягивать актуальный тариф с сайта IEC вместо ручного ввода. Проверили и отказались от идеи:

- Страница IEC рисует тариф через JavaScript в браузере — сырой HTML-запрос (что прямой, что из кода журнала) не содержит числа вообще.
- Даже если бы это не мешало — сработал бы **CORS**: браузер блокирует такие кросс-доменные запросы к чужому сайту независимо от того, вызван запрос автоматически или по кнопке.
- Чтобы реально прочитать цифру, нужен headless-браузер (Selenium/Playwright) — то есть отдельный сервис, который надо где-то регулярно гонять и чинить при любом изменении вёрстки сайта.
- К тому же "тариф" — не одно число: единый тариф, тариф теу"ز (по часам/сезонам), плюс возможные скидки у частного поставщика.

Тариф меняется пару раз в год по решению регулятора — не тот случай, где нужна инфраструктура. Решили оставить как есть: поле цены запоминает последнее введённое значение, при изменении тарифа достаточно один раз вписать новое число. Классический случай **YAGNI** (You Aren't Gonna Need It) — не строить то, что не понадобится.

## Текущий статус

- Журнал живёт в git, синхронизация между устройствами через JSONBin подтверждена рабочей.
- Экспорт/импорт CSV работают и остаются штатным способом бэкапа.
- Пробег per session пока не ведётся регулярно (неудобно снимать на ходу) — но поле уже готово, график расхода на 100 км появится, как только накопятся пары показаний одометра.
- Графики «100 км» и «Мощность» уже присутствуют в карусели как заглушки; наполнить их — следующий шаг. Расход кВтч/100 км считается по парам показаний одометра, средняя мощность — по кВтч и длительности сессий. Возможные будущие графики: привычки по дням/часам зарядки.

## Тема оформления: светлая (текущая) и тёмная (архив)

С v1.8 боевой сайт переведён на **светлую** тему. Прежняя **тёмная** палитра сохранена ниже — чтобы при желании быстро вернуться (заменить блок `:root` в `index.html` на тёмные значения).

**Тёмная тема (архив, до v1.8):**
```
--bg:#1a1815;
--panel:#232120;
--panel-2:#2b2826;
--line:rgba(232,228,220,0.09);
--line-strong:rgba(232,228,220,0.16);
--text:#e9e5dc;
--muted:#948d82;
--muted-2:#6b655c;
--copper:#c88a4a;
--copper-dim:rgba(200,138,74,0.16);
--led:#4fae72;
--led-dim:rgba(79,174,114,0.18);
--red:#c85a52;
--amber:#d4a24a;
--teal:#4a90a4;
--bar:#c88a4a;
--cost:#8f9bff;
```

**Светлая тема (текущая, с v1.8):**
```
--bg:#f4f1ea;
--panel:#ffffff;
--panel-2:#e9e4d8;
--line:rgba(40,36,30,0.12);
--line-strong:rgba(40,36,30,0.24);
--text:#211d17;
--muted:#544d42;
--muted-2:#6f6658;
--copper:#9a5f24;
--copper-dim:rgba(154,95,36,0.16);
--led:#2f7d4c;
--led-dim:rgba(47,125,76,0.18);
--red:#b34237;
--amber:#9a6b1e;
--teal:#2f6b7d;
--bar:#c69256;
--cost:#2f2f9e;
```

Обе темы используют одни и те же имена CSS-переменных, поэтому переключение — это замена значений в одном блоке `:root`, без правок разметки и логики. Цвета подобраны так, чтобы на светлом фоне надписи были тёмными и контрастными, столбики графика (медь) не сливались с линией стоимости (индиго `#2f2f9e`), а подписи кВтч читались почти чёрным.

## Сделано в 3.1–3.3

**3.1 — расход «из батареи» и kWh:**
- Убран `≈X кВт` из строки записи (мощность из недостоверной длительности).
- usable-ёмкость = 80.0 кВтч константой `USABLE_KWH` (Ioniq 5 84 kWh AWD; валовая 84, usable 80 по EV-Database).
- Новый график «Расход из батареи» (kWh/100🔋) — по просадке SoC (ΔSoC × 80 ÷ км × 100), как на приборке. Рядом прежний «из зарядки» (kWh/100⚡) — по отданной энергии. Разница = потери зарядки (~13%). Карусель: Расход · kWh/100⚡ · kWh/100🔋 · ₪/100 · Пробег.
- «кВтч» → «kWh» во всём интерфейсе (CSV-колонки оставлены со старыми именами для совместимости).

**3.2 — показ времени начала:** в строке журнала время начала показывается даже без окончания — «19:18–» с висящим дефисом.

**3.3 — ток заряда, убрано время окончания:**
- Добавлено обязательное поле **«ток A»** (после % after; автоподстановка последнего, старт 10). В строке показывается «10A», в CSV — колонка «Ток А».
- **Убраны поля времени окончания** (ЧЧ:ММ) — оно фиксировало момент извлечения пистолета, а не конец зарядки. Существующие записи с окончанием/длительностью сохранены (обратная совместимость), новых не вводится.
- Время начала теперь проверяется на наличие (страховка от случайного стирания).
- Из примера заметки убрано «14А».

## Ключевое наблюдение: почему время окончания недостоверно

Подтверждено на практике (скриншот X Smart): когда батарея достигает лимита (80%), приложение переходит в статус **SuspendedEV, 0 kW, 0 A** — зарядка фактически закончена. Но счётчик **Duration продолжает расти**, пока пистолет не извлечён. То есть «время окончания» в приложении = момент извлечения пистолета, а не конец набора энергии (машина может стоять заряженной часами). Поэтому длительность и всё, что из неё считалось (мощность), недостоверны — отсюда решения 3.1/3.3 (убрать мощность, убрать поле окончания).

## Сделано в 3.4: прогноз времени окончания

Вместо недостоверного фактического окончания в записи показывается **расчётное** ≈окончание (курсивом, после дефиса: «19:18–≈23:43»), считающее настоящий конец зарядки (переход в Suspended), а не извлечение пистолета. Функция `predictEnd(e)`; показывается, когда есть начало + SoC before + SoC after + ток.

- Формула: нужная энергия = (ΔSoC/100) × usable(80); мощность = ток_факт × U × 3 фазы × КПД; время = энергия ÷ мощность; окончание = начало + время (перенос через полночь).
- **Ток_факт** = номинал × 0.97 (ставишь 10 A → течёт ~9.7). Константа `CHG_I_FACTOR`.
- **Напряжение** `CHG_VOLTAGE` = 229 В (стабильно 226–232). **Фазы** `CHG_PHASES` = 3. **КПД** `CHG_EFF` = 0.87.
- Проверка на реальном примере: 48→80% при 10 A с 19:18 → ≈23:43 (факт: Suspended наступил ~23:51 — точность хорошая).
- Все параметры прогноза — константы вверху скрипта, при необходимости меняются там.

## Сделано в 3.5: порядок мета-строки записи

Порядок данных в строке журнала переставлен под логику ввода (важное и снимаемое с машины первым — вперёд, цена — в конец):
`пробег → SoC → ток → время(начало–≈прогноз) → цена`.
Было: цена первой. Стало, напр.: `1514 км · SoC 48→80% · 10A · 19:18–≈23:43 · 0.64 ₪/kWh`. Длительность старых записей (`8ч 0м`) — рядом с временем. Дата остаётся заголовком, kWh и стоимость — справа крупно. Менялся только порядок в мета-строке.

## Сделано в 3.6–3.7: защита от дублей и потерь при вводе

Проблема из практики: форма заполняется в начале зарядки на телефоне (считываешь с машины ОДО + SoC), занимает весь экран. После «Добавить запись» новая запись создаётся ниже, вне поля зрения — не видя подтверждения, легко нажать «Добавить» второй раз и получить дубль. Плюс отдельно — случайное удаление записи без возможности вернуть.

**3.6 — три меры:**
- **Отмена удаления.** После подтверждения «Да» вверху журнала на 8 секунд появляется полоска «Запись удалена · Отменить». Нажатие возвращает запись (и в облако). Удалённая запись хранится в памяти до истечения таймера. (Проверено — работает.)
- **Автопрокрутка к журналу** после добавления новой записи (`scrollIntoView` к панели `#clog-journal-panel`) — сразу видно результат.
- Подтверждение **«✓ Запись добавлена»** вместо тихого «Записано».

**3.7 — упрощение:** убрана кратковременная блокировка кнопки «Добавить» (в 3.6 гасла на 2 сек). Оказалась бессмысленной: при автопрокрутке к журналу кнопка уходит выше экрана, нажать повторно физически нельзя — прокрутка сама решает проблему двойного тапа.

Итог: причина дублей (не видно результата) устранена прокруткой; случайное удаление откатывается полоской отмены.

## Отложенная вилка (добавлена в 3.6): расширить undo

Сейчас отмена работает только для удаления. Возможное расширение — общий undo (в т.ч. для случайной правки записи). Пока YAGNI: удаление было главным риском, оно закрыто.

## Сделано в 3.8–3.10: объединение графиков расхода в stacked-столбик

Раньше в карусели было два отдельных графика «расход на 100 км»: «из зарядки» (по отданной энергии) и «из батареи» (по просадке SoC). «Из зарядки на 100 км» несуразен как самостоятельный график — смешивает зарядку (стоянка, деньги) с поездкой (км). Их объединили в один stacked-столбик.

**3.8 — объединение:**
- Вкладок стало 4: `Расход · kWh/100 · ₪/100 · Пробег` (было 5).
- Один stacked-столбик «kWh/100»: низ = расход **из батареи** (реальная поездка по ΔSoC×usable), верх = **потери** (добавка до «из зарядки»). Полная высота = из зарядки. Потери считаются из фактических данных месяца (из зарядки − из батареи), не из фиксированных 13%.

**3.9 — исправление отрисовки:** цифры вынесены из тела столбика (там наложение/нечитаемость на медном фоне). Столбцы без SoC (напр. Jul — SoC начали писать с августа) рисуются сплошным медным (чистый «из зарядки»), а не приглушённой верхушкой.

**3.10 — цвет и подписи (вариант B):**
- Цвета сегментов вписаны в медную тему, но со смыслом: низ — приглушённый **зелёный** `--batt:#5c8a5a` (из батареи), верх — **терракота** `--loss:#b06a4f` (потери). Не светофорный красный (он занят удалением и означал бы ложную тревогу — потери это норма).
- Две строки цифр **над** столбиком, в тон сегментам: зелёным «из батареи», терракотой «+потери» (со знаком +, т.к. надстройка). Из зарядки = сумма. Легенда: «из батареи · потери · kWh/100 км».
- Вырожденный случай: если месяц «живой» (энергия последней зарядки ещё 0), «из зарядки» занижена → потери схлопываются в 0/минус. Это не баг формулы, а неполные данные; как впишешь реальную энергию — потери станут нормальными (~13%). Проверено: август с 25 kWh дал +2.3 (~14%).

## Параметры темы (дополнение)

Добавлены в палитру `:root`: `--batt:#5c8a5a` (зелёный, расход из батареи), `--loss:#b06a4f` (терракота, потери зарядки). Обе — приглушённые, чтобы вписаться в медно-янтарную тему щитка и не конфликтовать с `--red` (удаление) и `--cost` (индиго, стоимость).
